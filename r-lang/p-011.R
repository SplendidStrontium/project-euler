# problem 11
# given a matrix, find a value

mymatr <- read.table("GitHub/project-euler/r-lang/p-011d.txt", header = FALSE)
print(mymatr[1,1])
bigprod <- 1

#left to right
for (row in 1:20) {
	print(mymatr[1,row])
	for (i in 1:17) {
	
	}
}